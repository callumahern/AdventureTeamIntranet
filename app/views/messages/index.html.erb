<% @messages.each do |message| %>
    <div class="container">
        <h2><%= message.title %></h2>
        <%= link_to "View Message", message_path(message), class: 'btn btn-dark' %>
    </div>
<% end %>

<br />
<br />

<%= link_to "New Message", new_message_path, class: 'btn btn-custom' %>