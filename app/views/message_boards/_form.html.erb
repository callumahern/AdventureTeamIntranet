<%= form_with(model: message_board) do |form| %>
  <% if message_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_board.errors.count, "error") %> prohibited this message_board from being saved:</h2>

      <ul>
        <% message_board.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :attachment %>
    <%= form.text_field :attachment %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
